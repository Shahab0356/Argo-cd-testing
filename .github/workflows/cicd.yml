name: CI/CD — Build, Push to Docker Hub & Deploy to K8s

on:
  push:
    branches: [ master ]

env:
  DOCKERHUB_USERNAME: shahabkhan
  IMAGE_NAME: flask-demo
  K8S_NAMESPACE: eks-demo-app
  DEPLOYMENT_NAME: eks-demo-app
  CONTAINER_NAME: eks-demo-app
  DOCKERFILE_PATH: ./docker/Dockerfile

permissions:
  contents: read

jobs:
  test-and-build:
    name: 🧪 Test, Build & Push to Docker Hub
    runs-on: ubuntu-latest
    outputs:
      IMAGE_TAG: ${{ steps.image-tag.outputs.tag }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r app/requirements.txt
          pip install pytest

      - name: Run tests
        run: pytest -q

      - name: 🔐 Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 🏷️ Generate image tag
        id: image-tag
        run: |
          DATE_TAG=$(date +%Y%m%d)
          SHORT_SHA=${GITHUB_SHA::7}
          TAG="${DATE_TAG}-${SHORT_SHA}"
          echo "tag=${TAG}" >> $GITHUB_OUTPUT

      - name: 🐳 Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ${{ env.DOCKERFILE_PATH }}
          push: true
          tags: |
            ${{ env.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ steps.image-tag.outputs.tag }}
            ${{ env.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:latest

  deploy:
    name: 🚀 Deploy to Kubernetes (Optional)
    runs-on: ubuntu-latest
    needs: test-and-build

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 🟡 Optional — only for AWS EKS
      - name: Configure AWS credentials (OIDC)
        if: env.AWS_REGION != ''
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/GitHubActionsEKSRole
          aws-region: us-east-1

      - name: Update kubeconfig (EKS only)
        if: env.AWS_REGION != ''
        run: aws eks update-kubeconfig --region us-east-1 --name shahab-eks-cluster

      - name: 🧱 Ensure namespace exists
        run: |
          kubectl get ns ${{ env.K8S_NAMESPACE }} || kubectl create ns ${{ env.K8S_NAMESPACE }}

      - name: 🚀 Deploy new Docker image
        run: |
          IMAGE_TAG="${{ needs.test-and-build.outputs.IMAGE_TAG }}"
          IMAGE="${{ env.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${IMAGE_TAG}"
          echo "Deploying image: $IMAGE"
          kubectl -n ${{ env.K8S_NAMESPACE }} set image deployment/${{ env.DEPLOYMENT_NAME }} \
            ${{ env.CONTAINER_NAME }}=$IMAGE --record

      - name: 🔍 Verify rollout
        run: |
          kubectl -n ${{ env.K8S_NAMESPACE }} rollout status deployment/${{ env.DEPLOYMENT_NAME }} --timeout=300s

      - name: 📊 Show Deployment Info
        run: |
          kubectl -n ${{ env.K8S_NAMESPACE }} get deployment ${{ env.DEPLOYMENT_NAME }} -o wide
          kubectl -n ${{ env.K8S_NAMESPACE }} get pods -o wide
